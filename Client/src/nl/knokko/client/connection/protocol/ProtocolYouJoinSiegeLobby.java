package nl.knokko.client.connection.protocol;

import nl.knokko.util.bits.BitInput;

public class ProtocolYouJoinSiegeLobby implements ReceiveProtocol {
	
	@Override
	public void process(BitInput input) {
		//ClientSiegeLobby lobby = new ClientSiegeLobby(input);
		//TODO create the necessary classes
	}
}